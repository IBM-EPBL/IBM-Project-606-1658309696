import random
n=random.randint(-20,60)
humidity=random.randint(1,100)
while(1):
    try:
        if n>=45 and humidity<50:
            print("HIGH TEMP and ALARM DETECTED")
        else:
            print("NOT DETECTED") 
    except KeyboardInterrupt:
        print("SENSOR STOPPED")
